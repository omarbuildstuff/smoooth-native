/// SmooothCore — framework-free, parity-locked logic ported from the original
/// Electron/TypeScript Smoooth app. Everything here is pure and unit-testable
/// via `swift test`, with no AppKit/AVFoundation dependencies.
public enum SmooothCore {
    public static let version = "0.1.0"
}
